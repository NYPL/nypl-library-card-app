version: "3"

services:
  web:
    container_name: nypl-library-card-app
    build:
      context: ./
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 3000:3000
