import React     from 'react';
import { Route } from 'react-router';
import ApplicationContainer from './../components/Application/ApplicationContainer';

export default (
  <Route name="app" component={ApplicationContainer} path="/">
  </Route>
);